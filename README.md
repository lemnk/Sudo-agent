# sudoagent

sudoagent is a small Python library that intercepts sensitive function calls, evaluates a policy, and can require interactive human approval. It writes an append-only JSONL audit log for every decision.

Status: v0.1 MVP

## Quickstart
```python
# Quickstart example coming soon

